<%= form_for(@slam) do |f| %>
  <% if @slam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slam.errors.count, "error") %> prohibited this slam from being saved:</h2>

      <ul>
      <% @slam.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :media_id_first_id %><br>
    <%= f.text_field :media_id_first_id %>
  </div>
  <div class="field">
    <%= f.label :media_id_second_id %><br>
    <%= f.text_field :media_id_second_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
